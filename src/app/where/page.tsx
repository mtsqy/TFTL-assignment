import Magnetic from "@/components/MagneticButton"

export default function Where() {
  return (
    <main>
        <section>
            <Magnetic>
                WHERE?
            </Magnetic>
        </section>
    </main>
  )
}